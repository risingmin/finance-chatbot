const config = {
    API_BASE_URL: 'http://localhost:5000/api', // Base URL for the backend API
    CHAT_ENDPOINT: '/chat', // Endpoint for chat interactions
    FINANCE_TOOLS_ENDPOINT: '/finance-tools', // Endpoint for finance tools
};

export default config;